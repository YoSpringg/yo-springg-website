const button = document.querySelector("#thanksButton");
const message = document.querySelector("#message");

button.addEventListener("click", function() {
    message.textContent = "Thanks for checking out Yo_Springg!";
});
